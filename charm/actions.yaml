# Copyright 2022 maksim
# See LICENSE file for licensing details.
#
# If you don't need actions, you can remove the file entirely.
# It ties in to `_on_drop_table_action` and `_on_get_db_info_action` handler in src/charm.py
#
# Learn more about actions at: https://juju.is/docs/sdk/actions

drop-table:
  description: Drops database table with names using app's API
  params:
    timeout:
      description: "API Request timeout"
      type: integer
      default: 10

get-db-info:
  description: Fetches Database relation information
